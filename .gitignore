# Build artifacts
/admiralbbs
*.exe
/dist/

# Runtime secrets & state — never commit these
ssh_host_*_key
ssh_host_*_key.pub
audit.jsonl
*.jsonl
*.db
*.db-wal
*.db-shm

# Local config / scratch
*.local
.DS_Store

# Go
/vendor/
